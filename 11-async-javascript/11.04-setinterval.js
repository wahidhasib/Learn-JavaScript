/**
 * ============================================================
 * 📘 setInterval()
 * ============================================================
 */

let count = 0;

/**
 * ============================================================
 * 🧩 Repeat Task
 * ============================================================
 */

const timer = setInterval(function () {
  count++;

  console.log("Count:", count);

  if (count === 5) {
    clearInterval(timer);
  }
}, 1000);

console.log("Hello js");

/**
 * ============================================================
 * 🎯 SUMMARY
 * ============================================================
 *
 * setInterval():
 * → বারবার execute হয়
 *
 * ============================================================
 */
