/**
 * ============================================================
 * 📘 Callback Hell
 * ============================================================
 */

setTimeout(() => {
  console.log("Step 1");

  setTimeout(() => {
    console.log("Step 2 ");

    setTimeout(() => {
      console.log("Step 3");

      setTimeout(() => {
        console.log("Step 4");
      }, 1000);
    }, 1000);
  }, 1000);
}, 1000);

/**
 * ============================================================
 * ⚠️ PROBLEM
 * ============================================================
 *
 * Nested callback code unreadable হয়ে যায়
 */

/**
 * ============================================================
 * 🎯 SUMMARY
 * ============================================================
 *
 * Callback hell:
 * → deeply nested async code
 *
 * ============================================================
 */
