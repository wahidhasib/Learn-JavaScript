```bash
07-arrays/
├── 7.01-array-basics.js                # Array কী এবং কিভাবে কাজ করে
├── 7.02-accessing-array-elements.js   # Array এর data/index access করার নিয়ম
├── 7.03-add-remove-elements.js        # Array তে data add এবং remove করা
├── 7.04-splice-vs-slice.js            # splice() এবং slice() এর পার্থক্য
├── 7.05-array-looping.js              # Loop ব্যবহার করে array iterate করা
├── 7.06-array-destructuring.js        # Array destructuring করে value বের করা
├── 7.07-spread-operator.js            # Spread operator দিয়ে copy/merge করা
├── 7.08-array-methods.js              # গুরুত্বপূর্ণ basic array methods
├── 7.09-map.js                        # map() দিয়ে data transform করা
├── 7.10-filter.js                     # filter() দিয়ে condition অনুযায়ী data বের করা
├── 7.11-reduce.js                     # reduce() দিয়ে data combine/sum করা
├── 7.12-find-findIndex.js             # find() ও findIndex() দিয়ে data খোঁজা
├── 7.13-some-every.js                 # some() ও every() দিয়ে condition check করা
├── 7.14-sort-reverse.js               # Array sort এবং reverse করা
├── 7.15-multidimensional-array.js     # Nested/Multidimensional array নিয়ে কাজ
├── 7.16-array-reference.js            # Array copy vs reference behaviour
└── 7.17-array-summary.js              # পুরো array chapter এর summary ও recap
```
