/**
 * ============================================================
 * 📘 Optional Chaining (?.)
 * ============================================================
 *
 * Nested property safely access করতে use হয়
 */

/**
 * ============================================================
 * 🧩 Example
 * ============================================================
 */

const user = {
  profile: {
    name: "Hasib",
  },
};

console.log(user.profile?.name);

console.log(user.address?.city);

/**
 * ⚠️ Error দিবে না
 */

/**
 * ============================================================
 * 🎯 SUMMARY
 * ============================================================
 *
 * ?. nested property safely access করে
 *
 * ============================================================
 */
