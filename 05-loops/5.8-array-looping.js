/**
 * ============================================================
 * 📘 Array Looping
 * ============================================================
 */

/**
 * ============================================================
 * 🧩 Traditional for loop
 * ============================================================
 */

const numbers = [10, 20, 30];

for (let i = 0; i < numbers.length; i++) {
  console.log(numbers[i]);
}

/**
 * ============================================================
 * 🧩 forEach()
 * ============================================================
 */

numbers.forEach(function (number) {
  console.log(number);
});

/**
 * ============================================================
 * 🧩 Arrow Function
 * ============================================================
 */

numbers.forEach((number) => {
  console.log(number);
});

/**
 * ============================================================
 * 🎯 SUMMARY
 * ============================================================
 *
 * Array loop করার multiple way আছে
 *
 * ============================================================
 */
