/**
 * ============================================================
 * 📘 Keyboard Events
 * ============================================================
 */

const input = document.getElementById("username");

/**
 * ============================================================
 * 🧩 keydown
 * ============================================================
 */

input.addEventListener("keydown", function (event) {
  console.log(event.key); // output (pressed key)
});

/**
 * ============================================================
 * 🧩 keyup
 * ============================================================
 */

input.addEventListener("keyup", function () {
  console.log("Key Released"); // fired when released the key
});

/**
 * ============================================================
 * 🧩 keypress
 * ============================================================
 */

input.addEventListener("keypress", function () {
  console.log("Typing...");
});

/**
 * ============================================================
 * 🎯 SUMMARY
 * ============================================================
 *
 * Keyboard interaction detect করা যায়
 *
 * ============================================================
 */
